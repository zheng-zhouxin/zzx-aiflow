version: '3'

services:
    zzx-aiflow-postgresql:
        image: postgres:18
        container_name: zzx-aiflow-postgresql
        ports:
            - '5433:5432'
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_DB=postgres
            # - POSTGRESQL_USERNAME=heyi
            - POSTGRES_PASSWORD=xiaoer
            # - POSTGRESQL_DATABASE=postgres
        volumes:
            - ./postgresql_data:/var/lib/postgresql
        # 日志控制，很重要防止服务器被日志文件爆满
        logging:
            driver: 'json-file'
            options:
                max-size: '100m'
                max-file: '3'

    # Qdrant 向量数据库
    zzx-aiflow-qdrant:
        image: qdrant/qdrant:latest
        container_name: zzx-aiflow-qdrant
        ports:
            - '6333:6333' # REST API
            - '6334:6334' # gRPC
        volumes:
            - ./qdrant_storage:/qdrant/storage
        logging:
            driver: 'json-file'
            options:
                max-size: '100m'
                max-file: '3'

networks:
    default:
        name: zzx-aiflow-network
        driver: bridge
